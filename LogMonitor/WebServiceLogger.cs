namespace LogMonitor
{
    internal sealed class WebServiceLogger: ILogger
    {
        public void LogErrorMessage(string errorMessage)
        {
            //TODO: Call the WS to log the errorMessage parameter.
        }
    }
}